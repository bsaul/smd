# SugarMaryDenver

An `R` package for computing the standardized mean difference between two groups for various data types. It is currently based on "A unified approach to measuring the effect size between two groups using SAS."

```
x <- rnorm(100)
g <- rep(1:2, each = 50)
smd(x, g)
```
